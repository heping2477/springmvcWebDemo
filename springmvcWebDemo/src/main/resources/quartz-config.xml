<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
    <beans>
        <!--定时任务 start-->


        <!-- 工作的bean -->
        <bean id="clearOnlineUJob" class="com.hp.controller.timeTask.job1" />

        <!-- job的配置开始 -->
        <bean id="myJobTask" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
            <!-- 调用的类 -->
            <property name="targetObject">
                <ref bean="clearOnlineUJob" />
            </property>
            <!-- 调用类中的方法 -->
            <property name="targetMethod">
                <value>say</value>
            </property>
        </bean>
        <!-- job的配置结束 -->

        <!-- 定义触发时间 --> <!-- quartz-2.x的配置 -->
        <bean id="myJobTrigger"
              class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
            <property name="jobDetail">
                <ref bean="myJobTask" />
            </property>
            <property name="cronExpression">
                <value>0 0 12 * * ?</value>
            </property>
        </bean>

        <bean id="myJobTrigger2"
              class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
            <property name="jobDetail">
                <ref bean="myJobTask" />
            </property>
            <property name="cronExpression">
                <value>0 0 11 * * ?</value>
            </property>
        </bean>

        <!-- 总管理类 如果将lazy-init='false'那么容器启动就会执行调度程序  -->
        <bean name="startQuertz" lazy-init="false" autowire="no"
              class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
            <property name="triggers">
                <list>
                    <ref bean="myJobTrigger" />
                    <ref bean="myJobTrigger2"/>
                </list>
            </property>
        </bean>

        <!--定时任务 end-->
    </beans>
</beans>